metadata:
  annotations:
    team: data-insights  # Updated to match your actual team name
    source-code: cloud-co  # Corrected to match the repository name
  name: test-namespace
spec:
  # Other namespace specifications go here