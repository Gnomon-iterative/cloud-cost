metadata:
  annotations:
    team: data-insights  # Updated to match your actual team name
    source-code: cloud-cost  # Corrected to match the repository name exactly
  name: test-namespac
spec:
  # Other namespace specifications go here