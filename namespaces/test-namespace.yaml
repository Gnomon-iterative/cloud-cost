metadata:
  annotations:
    team: data-insights  # Team that owns this namespace
    source-code: cloud-cost  # Repository containing the source code
    workflow-trigger: true  # Trigger workflow
  name: test-namespace  # Unique namespace name

spec:
  # Other namespace specifications go here